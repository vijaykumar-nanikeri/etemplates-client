import { AutoFocusNextSiblingElementDirective } from './auto-focus-next-sibling-element.directive';

describe('AutoFocusNextSiblingElementDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoFocusNextSiblingElementDirective();
    expect(directive).toBeTruthy();
  });
});
